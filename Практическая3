void dz1() {
    double S, p, n;
    cout  << "Введите переменны S, p, n через пробел: ";
    cin >> S >> p >> n;
    double r = p / 100.0;
    double m = (S * r * pow(1 + r, n)) / (12 * (pow(1 + r, n) - 1));
    cout << "m = " << m << endl;
}
